const saveMapWallEntities ={
["col","row","pos","height","opacity",{
  sides:{
    front || back:{frames:{group:["position",children:{
artMesh.geometry.parameters.width
artMesh.geometry.parameters.height
artMesh.scale.x
  artMesh.material.map.image.src ,
frameMesh
{frameMesh.geometry.parameters.shape}
{frameMesh.geometry.parameters.options}
fMaterial.color
frameMesh.material.color
frameMesh.material.map.image.src
    }}}
  }
}]
}
