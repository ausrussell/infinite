{
  "rules": {
    ".read": true,
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "publicGalleries": {
      "$uid": {
        ".write": "auth !== null"
      }
    }
  }
}